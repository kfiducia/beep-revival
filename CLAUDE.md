# beep-revival (beep-firmware)

Working rules for this repo live in **[AGENTS.md](./AGENTS.md)** — the public, secrets-free agent
guide (build, off-device tests + CI gates, where domain logic lives). Operator secrets are in a
gitignored `AGENTS.local.md`, never committed (this repo is public). Read AGENTS.md before changes.
