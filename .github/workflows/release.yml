name: Release

# Cut a signed Beep Revival firmware release. Push a tag like `v1.0.0` (or run
# manually), and this builds BOTH shippable images on a GitHub-hosted runner —
# AP1 (AirPlay-1 + Snapcast, the default) and AP2 (AirPlay-2, no Snapcast) — signs
# each sysupgrade with the usign OTA key (repo secret BEEP_OTA_SEC), and publishes a
# GitHub Release with both signed .bins + raw images + a first-flash initramfs +
# checksums + pubkey.
#
# One-time setup: add the private OTA key as a repo secret named BEEP_OTA_SEC
#   gh secret set BEEP_OTA_SEC < ~/beep-ota-signing/beep-ota.sec
# (Its public half lives in rootfs-overlay/etc/beep-ota.pub and is baked into
# every image, so devices verify the signature before flashing.)

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version string to stamp (e.g. v1.0.0-test); defaults to the run id'
        required: false

permissions:
  contents: write        # create the GitHub Release + upload assets

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-sign-release:
    runs-on: ubuntu-latest
    timeout-minutes: 300   # two full image builds; first run also builds the toolchain (~45-90 min)
    steps:
      - name: Reclaim runner disk (OpenWrt build needs ~25 GB)
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve version
        id: ver
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then V="${{ github.ref_name }}";
          elif [ -n "${{ github.event.inputs.version }}" ]; then V="${{ github.event.inputs.version }}";
          else V="0.0.0+ci.${{ github.run_number }}"; fi
          echo "version=$V" >> "$GITHUB_OUTPUT"
          echo "Building version $V"

      - name: Install build dependencies
        run: |
          sudo DEBIAN_FRONTEND=noninteractive apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            build-essential clang flex bison g++ gawk gettext git \
            libncurses-dev libssl-dev python3 python3-setuptools rsync swig \
            unzip zlib1g-dev file wget time bc quilt cmake

      - name: Prepare build root (/build) — matches the scripts' default paths
        run: sudo mkdir -p /build && sudo chown "$(id -u):$(id -g)" /build

      - name: Cache OpenWrt toolchain + downloads
        uses: actions/cache@v4
        with:
          path: |
            /build/openwrt/dl
            /build/openwrt/staging_dir
            /build/openwrt/build_dir/toolchain-*
          key: openwrt-v24.10.0-ath79-${{ hashFiles('scripts/docker-build-setup.sh') }}
          restore-keys: |
            openwrt-v24.10.0-ath79-

      - name: Set up OpenWrt + build cross-toolchain
        run: |
          set -e
          export FORCE_UNSAFE_CONFIGURE=1
          cd /build
          # Restoring the cache re-creates /build/openwrt/{dl,staging_dir,build_dir},
          # so a bare `[ -d openwrt ]` guard wrongly concludes "already cloned" and
          # skips the clone — leaving scripts/feeds missing (exit 127). Detect a REAL
          # checkout instead, and when cloning, clone into a temp dir and merge it over
          # the cache-restored subdirs so the downloads/toolchain cache is preserved.
          if [ ! -e openwrt/scripts/feeds ]; then
            rm -rf /build/openwrt.src
            git clone --depth 1 -b v24.10.0 https://git.openwrt.org/openwrt/openwrt.git /build/openwrt.src
            mkdir -p openwrt
            cp -a /build/openwrt.src/. openwrt/
            rm -rf /build/openwrt.src
          fi
          cd openwrt
          ./scripts/feeds update -a >/dev/null
          ./scripts/feeds install -a >/dev/null
          cat > .config <<'CFG'
          CONFIG_TARGET_ath79=y
          CONFIG_TARGET_ath79_generic=y
          CONFIG_TARGET_ath79_generic_DEVICE_8dev_carambola2=y
          CFG
          make defconfig >/dev/null
          # low -j for the toolchain (gcc bootstrap races at high parallelism)
          make -j"$(nproc)" tools/install toolchain/install

      - name: Stamp firmware version
        run: echo "Beep Revival ${{ steps.ver.outputs.version }}" > rootfs-overlay/etc/beep-version

      - name: Build both images (AP1 default + AP2); collect each before the next overwrites
        id: img
        run: |
          export OW=/build/openwrt SRC="$GITHUB_WORKSPACE"
          OUT=/build/openwrt/bin/targets/ath79/generic
          V="${{ steps.ver.outputs.version }}"
          mkdir -p /tmp/collected
          # build.sh writes a FRESH .config and restores the shairport/ffmpeg Makefiles
          # to pristine on every run, so these two builds don't leak into each other.
          echo "::group::AP1 build (AirPlay-1 + Snapcast, SSH OFF — no BEEP_DEV)"
          bash "$SRC/scripts/build.sh"
          echo "::endgroup::"
          cp "$(ls "$OUT"/*8dev_carambola2-squashfs-sysupgrade.bin)" "/tmp/collected/beep-revival-${V}-ap1-sysupgrade.bin"
          # ship one initramfs (from the default build) for the first-time UART RAM-boot
          cp "$(ls "$OUT"/*8dev_carambola2-initramfs-kernel.bin)"    "/tmp/collected/beep-revival-${V}-initramfs-kernel.bin"
          echo "::group::AP2 build (AirPlay-2, no Snapcast)"
          AIRPLAY2=1 bash "$SRC/scripts/build.sh"
          echo "::endgroup::"
          cp "$(ls "$OUT"/*8dev_carambola2-squashfs-sysupgrade.bin)" "/tmp/collected/beep-revival-${V}-ap2-sysupgrade.bin"
          echo "ap1=/tmp/collected/beep-revival-${V}-ap1-sysupgrade.bin" >> "$GITHUB_OUTPUT"
          echo "ap2=/tmp/collected/beep-revival-${V}-ap2-sysupgrade.bin" >> "$GITHUB_OUTPUT"
          echo "initramfs=/tmp/collected/beep-revival-${V}-initramfs-kernel.bin" >> "$GITHUB_OUTPUT"
          ls -la /tmp/collected/

      - name: Build usign (matches the baked-in key format)
        run: |
          git clone --depth 1 https://github.com/openwrt/usign /tmp/usign
          cmake -S /tmp/usign -B /tmp/usign/build >/dev/null
          make -C /tmp/usign/build >/dev/null
          /tmp/usign/build/usign -h 2>&1 | head -1 || true

      - name: Sign both sysupgrade images
        env:
          BEEP_OTA_SEC: ${{ secrets.BEEP_OTA_SEC }}
        run: |
          [ -n "$BEEP_OTA_SEC" ] || { echo "::error::secret BEEP_OTA_SEC is not set"; exit 1; }
          umask 077; printf '%s\n' "$BEEP_OTA_SEC" > /tmp/beep-ota.sec
          V="${{ steps.ver.outputs.version }}"
          for variant in ap1 ap2; do
            RAW="/tmp/collected/beep-revival-${V}-${variant}-sysupgrade.bin"
            SIGNED="/tmp/collected/beep-revival-${V}-${variant}-sysupgrade.signed.bin"
            USIGN=/tmp/usign/build/usign bash scripts/sign-release.sh "$RAW" /tmp/beep-ota.sec "$SIGNED"
            # verify our own signature against the public key baked into the image
            /tmp/usign/build/usign -V -m "$RAW" \
              -p rootfs-overlay/etc/beep-ota.pub \
              -x <(head -2 "$SIGNED") \
              && echo "$variant: signature verifies against baked-in pubkey"
          done
          shred -u /tmp/beep-ota.sec

      - name: Assemble release assets + checksums
        id: assets
        run: |
          V="${{ steps.ver.outputs.version }}"
          mkdir -p release
          cp /tmp/collected/beep-revival-${V}-ap1-sysupgrade.bin        release/
          cp /tmp/collected/beep-revival-${V}-ap1-sysupgrade.signed.bin release/
          cp /tmp/collected/beep-revival-${V}-ap2-sysupgrade.bin        release/
          cp /tmp/collected/beep-revival-${V}-ap2-sysupgrade.signed.bin release/
          cp /tmp/collected/beep-revival-${V}-initramfs-kernel.bin      release/
          cp rootfs-overlay/etc/beep-ota.pub release/beep-ota.pub
          ( cd release && sha256sum * > SHA256SUMS )
          ls -la release/

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          name: Beep Revival ${{ steps.ver.outputs.version }}
          tag_name: ${{ github.ref_type == 'tag' && github.ref_name || format('v{0}', steps.ver.outputs.version) }}
          draft: ${{ github.ref_type != 'tag' }}   # manual runs land as a draft to review
          generate_release_notes: true
          files: release/*
          body: |
            **Beep Revival firmware — ${{ steps.ver.outputs.version }}**

            Open OpenWrt-based firmware for the orphaned Beep "Dial" speaker (AR9331 / Carambola 2).

            ### Pick your image — AP1 (default) or AP2
            Two flavors ship each release; both accept an iPhone and both are signed.
            Switch flavors any time by flashing the other image — no re-provisioning.

            | Feature | `*-ap1-*` (default) | `*-ap2-*` |
            |---|---|---|
            | AirPlay from iPhone / iPad / Mac | ✅ | ✅ |
            | AirPlay protocol | AirPlay-1 (RAOP) | AirPlay-2 (also accepts AirPlay-1 senders) |
            | Audio codec | ALAC — **lossless** | AAC 256k — lossy |
            | **Snapcast** synced multi-room | ✅ (our multi-room) | ❌ (dropped for size) |
            | Native AirPlay-2 multi-room grouping | ❌ | ✅ (CPU-marginal) |
            | Apple **Home** app (tiles, Siri, automations, stereo-pair) | ❌ | ✅ |
            | CPU headroom on the AR9331 | comfortable | **thin** — can glitch under load |
            | Knob / web volume, Wi-Fi setup AP, signed OTA, recovery | ✅ | ✅ |
            | Recommended for | **most users** | only if you want Home-app integration |

            **Recommendation:** flash **`*-ap1-sysupgrade.signed.bin`** unless you specifically
            need the Apple Home app — AP1 is lossless, lighter, and keeps Snapcast multi-room.

            ### Install (already on Beep Revival)
            In the device's web admin → **Update firmware**, upload your chosen
            **`*-sysupgrade.signed.bin`**. The device verifies the usign signature
            against its baked-in key before flashing. No unsigned bypass without the
            physical triple-tap.

            ### First-time flash (still on stock/cloud firmware)
            You need a 3.3 V USB-UART adapter. Follow **[docs/FLASHING-WALKTHROUGH.md](https://github.com/${{ github.repository }}/blob/${{ github.ref_name }}/docs/FLASHING-WALKTHROUGH.md)**.
            RAM-boot the `*-initramfs-kernel.bin`, then sysupgrade the `*-ap1-sysupgrade.bin` (or ap2).

            ### Verify a download
            `usign -V -m <image> -p beep-ota.pub -x <(head -2 *.signed.bin)` — or check `SHA256SUMS`.

            > ⚠️ **No warranty.** 3.3 V UART only. Never write `u-boot` or `art`. See the walkthrough's safety section.
